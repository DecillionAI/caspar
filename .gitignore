machines/docker/appbox/src/credentials.json
# Rust build artifacts
target/
**/target/
Cargo.lock.bak

# Stray RocksDB dirs that caspar-node creates if it's launched from the
# repo root without STORAGE_ROOT_PATH / TELEMETRY_DB_PATH set
telemetry-rocks/
