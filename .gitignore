machines/docker/appbox/src/credentials.json
# Rust build artifacts
target/
**/target/
Cargo.lock.bak

# Node runtime telemetry store (created in cwd at runtime)
telemetry-rocks/

# VM plugin projects build under the node workspace lock; standalone-check
# lockfiles are local artifacts
vms/*/Cargo.lock
vm-sdk/Cargo.lock
